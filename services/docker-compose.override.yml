# Docker compose de desenvolvimento

services:
  farmoo-animais:
    build:
      context: ./animais
      dockerfile: Dockerfile.dev
    environment:
      CHOKIDAR_USEPOLLING: "true"
    develop:
      watch:
        # Sincroniza o código-fonte: alterações refletem na hora
        - action: sync
          path: ./animais/src
          target: /app/src
        # Rebuilda quando package.json ou package-lock.json mudam
        - action: rebuild
          path: ./animais/package.json
        - action: rebuild
          path: ./animais/package-lock.json
          
  farmoo-producoes:
    build:
      context: ./producoes
      dockerfile: Dockerfile.dev
    environment:
      CHOKIDAR_USEPOLLING: "true"
    develop:
      watch:
        # Sincroniza o código-fonte: alterações refletem na hora
        - action: sync
          path: ./producoes/src
          target: /app/src
        # Rebuilda quando package.json ou package-lock.json mudam
        - action: rebuild
          path: ./producoes/package.json
        - action: rebuild
          path: ./producoes/package-lock.json

  farmoo-financeiro:
    build:
      context: ./financeiro
      dockerfile: Dockerfile.dev
    environment:
      CHOKIDAR_USEPOLLING: "true"
    develop:
      watch:
        # Sincroniza o código-fonte: alterações refletem na hora
        - action: sync
          path: ./financeiro/src
          target: /app/src
        # Rebuilda quando package.json ou package-lock.json mudam
        - action: rebuild
          path: ./financeiro/package.json
        - action: rebuild
          path: ./financeiro/package-lock.json